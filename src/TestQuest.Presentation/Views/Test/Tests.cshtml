@using TestQuest.DataAccess;
@model IEnumerable<TestDto>;
@{
    ViewData["Title"] = "Tests";
    int i = 0;
}
@* @foreach(var ii in Model)
{
    <a href="test/@ii.Id" class="ii">@ii.Name</a><br/>
} *@
<form method="POST">
	<label for="search">Search</label>
	<input class="search" id="search" type="search" pattern=".*\S.*" name="searchTestWithName">
	<span class="caret"></span>
</form>

<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.6.4/dist/jquery.slim.min.js"></script>
    <link rel="stylesheet" href="~/css/tests.css" asp-append-version="true" />
</head>
<table class="table">
    <thred>
        <th>№</th>
        <th>Name</th>
    </thred>
    @foreach(var ii in Model)
    {
        <tbody> 
            <td>@{i++;}@i</td>
            <td><a href="/user/passingtest/@ii.Id" class="ii">@ii.Name</a></td>
        </tbody>
    }
</table>
